# Data, causality, normalization and input coverage audit

Read-only source/JSON audit of active frozen source `/home/andre2/src/GX1_FULL_TRAIN_NATIVE_DISPATCH_V33`, commit `2959cd09f34b20fe82f351f97f7b051b49918999`, and recipe `NATIVE_FULL_TRAIN_RECIPE_DISPATCH_V4.json`. Data root below means `/home/andre2/GX1_DATA/data/data/prebuilt/LIFECYCLE_V2_FULL_TRAIN_20260912`. No trainer interruption, remote edits, checkpoint/torch/parquet loads, or heavy verification was performed.

## Conclusion

No verified training-invalidating data leakage, input omission, or clock-alignment defect was found in the inspected active path. This is a source/metadata conclusion, not a fresh numerical replay of all historical features. Three material interpretation limits remain: older TRAIN has thousands of censored source gaps; cost/calendar authorities are prospective/inferred rather than proven historical truth; June VAL has already been used and is not an untouched final test.

The active model uses the preserved **v7 base normalization**, not the newly fit v8 base transform carried by the full child composite. This is explicit seed-preservation code and remains TRAIN-only. It must be reported accurately, but is not evidence of an invalid run.

## 1. Population, split boundaries, entry fill, and TEST isolation

- Full TRAIN index: **313,399 entries**, parent count also 313,399; requested window `[2021-06-01, 2026-06-01)`. `ENTRY_WINDOW/train.manifest.json`, `RANDOM_ACCESS_INDEX_FULL_TRAIN_V1/train.manifest.json` and root agree. The actual last entry is **2026-05-29 14:50 UTC**; underlying M1 outcome tape continues to May 31 23:59. “Five years through May 31” describes the allowed window, not entries on every calendar day.
- June VAL: **5,508 entries**, `[2026-06-01, 2026-07-01)`. Its parent has 5,509 rows because of a May 31 23:55 context row. Child filtering and explicit parent-coordinate mapping prevent this context row becoming a June evaluation entry.
- Runtime enforcement, not just manifests: `gx1/scripts/run_unified_exit_random_access_full_train_v1.py:211–218` checks the five-year window; `:232–250` constructs only TRAIN and VAL datasets/corpus; `:254–264` requires all child IDs and every parent TRAIN ID exactly once; `:276–280` selects the full-population schedule and binds parent/child coordinates. VAL cohort size and bindings are checked at `:283–307`.
- Fill clock is causal: `gx1/contracts/unified_exit_random_access_index_v1.py:218` computes first M1 state bar start as **entry M5 bar start +300 seconds**, checked at `:288–294`. `FINAL_BINDINGS_V1/FIRST_STATE_ENTRY_BRIDGE_TRAIN.json` binds `fill_source=first_exit_m1_bar_bid_open_ask_open`. Thus M5 `[t,t+5m)` is observed before fill at M1 open `t+5m`; first Exit decision is at `t+6m`. There is no same-bar close-feature/lookahead fill in this convention.
- TRAIN child M1 tape spans May 31, 2021 11:00 through May 31, 2026 23:59, including 474 preceding context rows; its fit window starts June 1. VAL child contains 474 preceding context rows and June data through June 30 23:59. Past context across a split is causal. TRAIN never receives June successor tape via the child mapping.
- `gx1/contracts/unified_exit_dataset_adapter_v2.py:582–609` proves exact parent and child clocks and slices feature arrays to that exact child clock. A feature timestamp offset or missing row does not silently align approximately.
- The full-train consumer requests only `splits=("train","val")`; `gx1/contracts/unified_exit_lifecycle_v1.py:1993–2039` avoids TEST artifact enumeration for this pretest authority. All inspected normalization/registry/cost manifests report zero TEST fitting or access. This does not prove all historical experiment activity never inspected TEST, which would require separate historical access evidence.

## 2. Feature generation and MTF causality

- Local M1 surface is the hash-bound `.../V9_FIVE_YEAR_ENTRY_NOTIONAL_20260905T132535Z/m1_feature_base.parquet`, 2,261,656 rows, 238 signal fields, 71 continuous context fields and 1 categorical context field; first valid feature row February 3, 2020, comfortably preceding full-TRAIN warmup.
- M1 materialization uses the same native owner implementation as M5, with native M1 inputs. `gx1/scripts/materialize_entry_exit_m1_feature_base_v1.py:314–371` calls the shared extension owner with precomputed native layers and explicit recursive foundation-event-age state carry. It does not resample computed M5 features into local M1 features.
- M1 loading consumes all three exact surface columns and validates width/dtypes in `gx1/contracts/entry_exit_feature_surface_v1.py:626–694`; mmap/page-discard comments describe storage handling, not feature selection.
- Actual state construction (`gx1/contracts/unified_exit_random_access_state_view_v1.py:301–423`) slices local history only through the current closed M1 row, takes current-row context, takes trade-path tail only through current state, and requests MTF history using the M1 **bar start**. Lifetime summary queries stop at `entry_start+state_index+1` (`unified_exit_dataset_adapter_v2.py:801–849`), including only elapsed path extrema. Its elapsed time includes the first 60 seconds after entry.
- Runtime Exit MTF materialization (`gx1/models/entry_v10/entry_v10_ctx_train_v3.py:4824–4889`) applies one +60-second availability shift, then subtracts each timeframe duration and uses `searchsorted(..., side="right")`. This admits exactly bars whose close is no later than the M1 decision. There is no double close shift.
- The MTF owner aggregates observed OHLCV before computing features (`gx1/features/htf_features.py:95–119`, `:3680–3753`). It uses fixed UTC grids, with H4/D1 phased to the declared 22:00 UTC session origin (`:875–963`), and fixed durations (`:918–924`). This is a consistent contracted clock; it is not a claim that every broker daily candle uses the same DST-dependent boundary.
- Scalar HTF context uses the same causal cutoff (`htf_features.py:3892–3938`), so local context does not sneak in a still-open HTF row.
- Reviewed a common lookahead hazard directly: `gx1/features/swing_structure_v1.py:177–251` computes symmetric pivots but only exposes pivot `j` from `j+lookback`, rather than retroactively marking row `j`. Targeted scans of active shared HTF/native layer owners found no negative-shift/backfill/centered-window shortcut.
- Existing TRAIN reconstruction evidence binds every emitted entry sequence/snapshot to exact source history, bit-identical: `ENTRY_SEQUENCE_SOURCE_RECONSTRUCTION_TRAIN_20260905T143732Z.json`, 313,399 ×96×238, PASS. This old verified evidence was reused, not rerun. Source/metadata review cannot independently certify every formula in every generated artifact without numerical replay.

## 3. Entire contracted input coverage

“200 features” is not the actual active width. The signal contract contains **26 base +145 mandatory +67 available selected =238** local signal fields. It is not selecting an arbitrary reduced 200-field subset.

- Local Entry history: 96 M5 rows ×238; Exit local history: 480 M1 rows ×238; current context: 71 continuous +1 categorical.
- MTF: **176 fields per timeframe**, with lengths M5=16, M15=64, H1=96, H4=96, D1=252. Entry MTF uses M15/H1/H4/D1 (its local source is M5); Exit MTF uses all five. Entry has 32 family/timeframe tokens and Exit40.
- The 176 MTF fields partition exactly once across eight families: structure20, SMC/liquidity43, trend22, volatility7, momentum20, session4, chart38, candle22. Metadata enumeration found zero omitted indices and zero duplicates. All71 continuous and the1 categorical context fields also have exactly one owner.
- Actual model rejects omitted/overlapping local indices (`gx1/models/entry_v10/entry_v10_ctx_hybrid_transformer.py:846–880`) and omitted/overlapping MTF indices (`:1009–1040`). Forward code gathers every declared local family (`:1965–1982`) and each declared MTF family (`:2061–2101`). Gates may learn low influence; that is different from an input being disconnected. Numerical influence/gradient movement is outside this source-only subaudit.

## 4. Normalization and warm-start scope

- New child base v8 hash: `3ebccfb84ba3b5baebe4c59e0fb09c9ce20f1e531c4f57749d9fbbd19b1be0e8`. Its declared fit uses TRAIN only through May31 23:59, 0 VAL/TEST rows. Earlier historical rows in long MTF windows are causal context, not future data.
- **Actual active base normalization is seed v7**, hash `efa41d82c7ba9464b9df3dbb51382979f7cb3c86d5a646057b88afb37901313d`: full train constructor builds the model with schema-compatible child v8, loads completed smoke EMA including its buffers, then binds old v7 (`run_unified_exit_random_access_full_train_v1.py:337–356`) and returns `input_normalization=old_norm` (`:384`). `unified_exit_random_access_model_v1.py:124–164` checks field, alias and categorical identity and verifies restored buffer state.
- V7 also covers the **full five-year TRAIN**, ending May31 23:45; signal fit count2,119,495 and context fit count2,077,822, 0 VAL/TEST fit. It is not a normalizer fit only on the one-year seed campaign. V7 and v8 numerical statistics differ (e.g.75 signal centers and87 scales), so descriptions must not say the new full child v8 transform is active.
- Lifetime-summary normalization is the full TRAIN child transform (`edb729d5...`), fit on4,414,336 outcome-blind physical samples. Adapter validates it against its summary manifest. The prior seed summary normalizer differed; changing normalization alters the starting input distribution but subsequent full training can adapt. No measured invalidation follows from that fact alone.
- Registry thresholds/volatility state parameters were fitted on historical TRAIN ending May2025 (registry inner fit ending February2025), before current June2026 VAL. Freezing an earlier TRAIN subset is not VAL leakage or feature omission.
- Seed authority binds completed one-year campaign:65,295 entry pairs,4,081 optimizer steps, epoch complete, EMA. Original source bundle already ran one training/validation epoch against the same June VAL; June was again evaluated in the completed full VAL run before this main training. June remains development validation, not an untouched test. There is no observed VAL gradient update in the inspected constructor; weight selection/recipe development can still produce selection bias on repeatedly viewed VAL.
- Smallest action: record actual preserved-v7 normalization and reused-VAL status in the final result. No change/restart of this active run is justified solely to substitute v8.

## 5. Gaps, calendar and economics coverage limits

- `CLOSURE_AUTHORITY_TRAIN_V2/market_closure_authority.json`:4,285 observed gaps,1,149 known market closures, **3,136 unknown source absences**, handled as `right_censor_before_gap`. Unknown counts by year:2021=1,985;2022=583;2023=371;2024=138;2025=52;2026=7. Older historical outcome paths therefore censor much more often than recent ones. Full entry coverage is not complete uninterrupted outcome coverage.
- June VAL:23known closures,0unknown gaps. TRAIN's schedule declares `source_method=project_inferred_pretest_closure_policy_v1`; some long holiday intervals remain unknown. Neither optimistic synthesis nor a verified broker historical calendar should be inferred from its PASS.
- Actual state-view guard (`unified_exit_random_access_state_view_v1.py:81–115`) raises if a non-contiguous successor lacks an explicitly permitted gap interval. TRAIN/VAL split ends are right censors and manifests contain0economic terminals. No evidence was found that unknown source gaps are silently crossed as ordinary observations.
- Prospective cost authority covers June1,2021–July1,2026 and declares **historical_cost_truth_qualified=false** and **economics_pass_claimed=false**. Active assumptions: side-correct bound bid/ask, commission0, slippage2bps/execution (VAL sensitivities1/2/4), long annual financing5.4%, short0, GSLO fee0 under no-GSLO policy, annual capital hurdle10%. This supports scenario training/evaluation, not a claim of verified historical broker net PnL.
- Separate economics agent identified potential omitted first-minute financing. This audit confirms first Exit decision is60seconds after filled Entry and lifetime-summary elapsed-time owner includes that60seconds (`unified_exit_dataset_adapter_v2.py:832–839`). Fill timing itself is causal. The accounting judgment and smallest correction belong to that finding's economic-provider audit.

## Remaining uncertainty / no invasive follow-up

This audit deliberately did not load checkpoints, parquets or feature matrices, or recompute historical signals; no data-dependent claim above is presented as a new full numerical proof. Existing bound reconstruction evidence plus actual loader/clock/index/forward source makes a silent feature truncation or obvious split-lookahead defect unlikely, but does not prove predictive validity, absence of all historical selection bias, or faithful real execution economics. Preserve the running trainer and evaluate the final measured result with these limits stated.
