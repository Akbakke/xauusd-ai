# Full VAL control repair

Epoch 1 completed 1024 optimizer steps at frozen execution source adce40828c9f9ce9cd843a2e80158da5c939ab12. The final checkpoint and final TRAIN authority remain immutable.

The first full VAL controller exited on Windows BootId 371 before ACTIVE, guard startup or evaluator execution. Windows error event 4100 records the 30000 ms metadata timeout. A bounded CPU-only measurement found 17.524 s for inspect plus 16.154 s for the repeated plan load in begin (33.678 s total).

Begin now loads the authority once while retaining the clean-source, receipt, reboot, pointer and fresh-output validations. Begin errors are included in the existing bootstrap error receipt. Successful full VAL recording resolves COMPLETE or RESUMABLE from validated terminal progress; a nonzero child status cannot request automatic success.

The plan's existing absolute controller source bindings can select this repaired control checkout. The installer passes a separate metadata working directory, and the CLI verifies its executing file against the plan. Model execution, guards, data, checkpoint, launch and source commit remain bound to the frozen adce checkout. No learning or feature source changed.

Validation: the production campaign materializer test exercises a separate control repository, a single begin plan load, resumable and complete VAL receipts, and rejection of automatic success after a failed child. The CLI identity regression rejects wrong paths and changed bytes. The Windows harness executes the production dispatch and outcome selection on the actual host.

This repair establishes no VAL quality result or positive net Bps. Full VAL remains the next model operation.
