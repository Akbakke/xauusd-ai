Retired exit configs (not used in canonical ENTRY_V10_CTX training).

- FARM_EXIT_FIXED_BARS_FIXED.yaml was moved here. Canonical pipeline is supervised only (no fixed-bar exits).

Do not use these configs unless explicitly re-enabled after review.
